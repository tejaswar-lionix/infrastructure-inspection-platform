[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "infrastructure-inspection-platform"
version = "1.0.0"
description = "Infrastructure Inspection Platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}
